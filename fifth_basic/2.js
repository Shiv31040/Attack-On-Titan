

let index = 0
while (index<=10) {
    // console.log(`value of index is ${index}`);
    index = index+2
}


let arr1 = ["shivam","sharma","is", "a", "boy"]
let arr_value = 0
while (arr_value<arr1.length) {
    // console.log(`${arr1[arr_value]}  `);
    arr_value++
}

let score = 11
do {
    console.log(`score is ${score}`);
    score++
} while (score<=10);